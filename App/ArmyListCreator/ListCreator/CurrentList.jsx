import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const CurrentList = () => {
  return (
    <View>
      <Text>Contains all the current units in this army. Contains a number of different nationss</Text>
      <Text>User can click each force and either delete, view, copy the force</Text>
    </View>
  )
}

export default CurrentList

const styles = StyleSheet.create({})